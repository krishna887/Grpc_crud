micronaut:
  application:
    name: grpcproject
  server:
    port: 8080
datasources:
  default:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/todo
    username: postgres
    password: root
jpa:
    default:
      properties:
        hibernate:
          hbm2ddl:
            auto: update
          show_sql: true


